fn main(secret: Field, #[public] expected: Field) {
    // গোপন সংখ্যার বর্গফল গণনা করা হচ্ছে
    let square = secret * secret;
    
    // যাচাই করা হচ্ছে যে গণিতকৃত বর্গফলটি expected মানের সমান কিনা
    assert(square == expected);
}
